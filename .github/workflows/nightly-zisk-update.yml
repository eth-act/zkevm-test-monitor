name: Nightly Zisk ZKVM Update

on:
  schedule:
    # Run daily at 3:30 AM UTC (30 minutes after Jolt to avoid conflicts)
    - cron: '30 3 * * *'
  workflow_dispatch:  # Allow manual trigger for testing

permissions:
  contents: write
  actions: read

concurrency:
  group: zisk-update
  cancel-in-progress: true

jobs:
  update-zisk:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: Set up Git configuration
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq python3 python3-pip
          pip3 install pyyaml
      
      - name: Get latest Zisk commit
        id: zisk-commit
        run: |
          # Get the current commit hash from config.json
          CURRENT_COMMIT=$(jq -r '.zkvms.zisk.commit' config.json)
          echo "Current Zisk commit: $CURRENT_COMMIT"
          
          # Get the latest commit hash from the main branch of the remote repository
          REPO_URL=$(jq -r '.zkvms.zisk.repo_url' config.json)
          LATEST_COMMIT=$(git ls-remote $REPO_URL refs/heads/main | cut -f1 | head -c8)
          echo "Latest Zisk commit from main branch: $LATEST_COMMIT"
          
          # Check if there's an update available
          if [ "$CURRENT_COMMIT" != "$LATEST_COMMIT" ]; then
            echo "needs_update=true" >> $GITHUB_OUTPUT
            echo "current_commit=$CURRENT_COMMIT" >> $GITHUB_OUTPUT  
            echo "latest_commit=$LATEST_COMMIT" >> $GITHUB_OUTPUT
            echo "🔄 Zisk update available: $CURRENT_COMMIT -> $LATEST_COMMIT"
          else
            echo "needs_update=false" >> $GITHUB_OUTPUT
            echo "✅ Zisk is up to date at commit $CURRENT_COMMIT"
          fi
      
      - name: Update config.json with latest commit
        if: steps.zisk-commit.outputs.needs_update == 'true'
        run: |
          LATEST_COMMIT=${{ steps.zisk-commit.outputs.latest_commit }}
          echo "📝 Updating config.json with commit: $LATEST_COMMIT"
          
          # Update the commit hash in config.json using jq
          jq --arg commit "$LATEST_COMMIT" '.zkvms.zisk.commit = $commit' config.json > config.json.tmp
          mv config.json.tmp config.json
          
          # Verify the update
          NEW_COMMIT=$(jq -r '.zkvms.zisk.commit' config.json)
          echo "✅ Updated Zisk commit to: $NEW_COMMIT"
      
      - name: Set up Docker Buildx
        if: steps.zisk-commit.outputs.needs_update == 'true'
        uses: docker/setup-buildx-action@v3
      
      - name: Build Zisk ZKVM
        if: steps.zisk-commit.outputs.needs_update == 'true'
        run: |
          echo "🔨 Building Zisk ZKVM..."
          # Force rebuild by removing existing binary
          rm -f binaries/zisk-binary
          
          # Run the build script specifically for Zisk
          FORCE=1 ./src/build.sh zisk
        continue-on-error: true
        id: build-zisk
      
      - name: Test Zisk ZKVM
        if: steps.zisk-commit.outputs.needs_update == 'true' && steps.build-zisk.outcome == 'success'
        run: |
          echo "🧪 Testing Zisk ZKVM..."

          # Run tests specifically for Zisk
          ./src/test.sh --arch zisk
        continue-on-error: true
        id: test-zisk
      
      - name: Update dashboard
        if: steps.zisk-commit.outputs.needs_update == 'true'
        run: |
          echo "📊 Updating dashboard..."
          python3 src/update.py
        continue-on-error: true
        id: update-dashboard
      
      - name: Check for changes and commit
        if: steps.zisk-commit.outputs.needs_update == 'true'
        run: |
          # Check if there are any changes to commit
          if git diff --quiet && git diff --cached --quiet; then
            echo "No changes to commit"
            echo "has_changes=false" >> $GITHUB_ENV
          else
            echo "Changes detected, preparing to commit"
            echo "has_changes=true" >> $GITHUB_ENV
            
            # Show what changed
            echo "📋 Files changed:"
            git diff --name-only
            git diff --cached --name-only
          fi
      
      - name: Commit and push changes
        if: steps.zisk-commit.outputs.needs_update == 'true' && env.has_changes == 'true'
        run: |
          CURRENT_COMMIT=${{ steps.zisk-commit.outputs.current_commit }}
          LATEST_COMMIT=${{ steps.zisk-commit.outputs.latest_commit }}
          
          # Add all modified files (only tracked files that should be committed)
          git add config.json data/ docs/ index.html || true
          
          # Create commit message with build/test status
          COMMIT_MSG="chore: update Zisk ZKVM from $CURRENT_COMMIT to $LATEST_COMMIT"
          
          if [ "${{ steps.build-zisk.outcome }}" = "success" ]; then
            COMMIT_MSG="${COMMIT_MSG}"$'\n\n'"✅ Build: successful"
          else
            COMMIT_MSG="${COMMIT_MSG}"$'\n\n'"❌ Build: failed"
          fi
          
          if [ "${{ steps.test-zisk.outcome }}" = "success" ]; then
            COMMIT_MSG="${COMMIT_MSG}"$'\n'"✅ Tests: completed"
          elif [ "${{ steps.build-zisk.outcome }}" = "success" ]; then
            COMMIT_MSG="${COMMIT_MSG}"$'\n'"❌ Tests: failed"
          else
            COMMIT_MSG="${COMMIT_MSG}"$'\n'"⚠️ Tests: skipped (build failed)"
          fi
          
          if [ "${{ steps.update-dashboard.outcome }}" = "success" ]; then
            COMMIT_MSG="${COMMIT_MSG}"$'\n'"✅ Dashboard: updated"
          else
            COMMIT_MSG="${COMMIT_MSG}"$'\n'"❌ Dashboard: update failed"
          fi
          
          COMMIT_MSG="${COMMIT_MSG}"$'\n\n'"🤖 Automated nightly update"
          
          # Commit changes
          git commit -m "$COMMIT_MSG"
          
          # Push changes
          git push origin main
          
          echo "✅ Changes committed and pushed successfully"
      
      - name: Create issue on failure
        if: failure() && steps.zisk-commit.outputs.needs_update == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const current_commit = '${{ steps.zisk-commit.outputs.current_commit }}';
            const latest_commit = '${{ steps.zisk-commit.outputs.latest_commit }}';
            const run_url = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            
            // Check if there's already an open issue for failed Zisk updates
            const issues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'automated,zisk-update,failed'
            });
            
            if (issues.data.length === 0) {
              // Create new issue only if none exists
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: `🚨 Nightly Zisk ZKVM Update Failed`,
                labels: ['automated', 'zisk-update', 'failed'],
                body: [
                  '## Zisk ZKVM Update Failed',
                  '',
                  `**Attempted Update:** \`${current_commit}\` → \`${latest_commit}\``,
                  '',
                  `**Workflow Run:** ${run_url}`,
                  '',
                  '### Status Summary',
                  `- **Build Status:** ${{ steps.build-zisk.outcome }}`,
                  `- **Test Status:** ${{ steps.test-zisk.outcome }}`,
                  `- **Dashboard Update:** ${{ steps.update-dashboard.outcome }}`,
                  '',
                  '### Next Steps',
                  `1. Check the [workflow run](${run_url}) for detailed error logs`,
                  '2. Investigate build/test failures',
                  '3. Fix any issues and manually trigger the workflow',
                  '4. Close this issue once resolved',
                  '',
                  'This issue was created automatically by the nightly update workflow.'
                ].join('\n')
              });
              console.log('Created issue for failed Zisk update');
            } else {
              console.log('Issue already exists for failed Zisk updates');
            }
      
      - name: Post job summary
        if: always()
        run: |
          echo "## Nightly Zisk ZKVM Update Summary" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.zisk-commit.outputs.needs_update }}" = "true" ]; then
            echo "### Update Status: 🔄 Update Available" >> $GITHUB_STEP_SUMMARY
            echo "- **From:** \`${{ steps.zisk-commit.outputs.current_commit }}\`" >> $GITHUB_STEP_SUMMARY
            echo "- **To:** \`${{ steps.zisk-commit.outputs.latest_commit }}\`" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Build status
            if [ "${{ steps.build-zisk.outcome }}" = "success" ]; then
              echo "### 🔨 Build: ✅ Successful" >> $GITHUB_STEP_SUMMARY
            else
              echo "### 🔨 Build: ❌ Failed" >> $GITHUB_STEP_SUMMARY
            fi
            
            # Test status
            if [ "${{ steps.test-zisk.outcome }}" = "success" ]; then
              echo "### 🧪 Tests: ✅ Completed" >> $GITHUB_STEP_SUMMARY
            elif [ "${{ steps.build-zisk.outcome }}" = "success" ]; then
              echo "### 🧪 Tests: ❌ Failed" >> $GITHUB_STEP_SUMMARY
            else
              echo "### 🧪 Tests: ⚠️ Skipped (build failed)" >> $GITHUB_STEP_SUMMARY
            fi
            
            # Dashboard status
            if [ "${{ steps.update-dashboard.outcome }}" = "success" ]; then
              echo "### 📊 Dashboard: ✅ Updated" >> $GITHUB_STEP_SUMMARY
            else
              echo "### 📊 Dashboard: ❌ Update failed" >> $GITHUB_STEP_SUMMARY
            fi
            
            # Commit status
            if [ "${{ env.has_changes }}" = "true" ]; then
              echo "### 📝 Changes: ✅ Committed and pushed" >> $GITHUB_STEP_SUMMARY
            else
              echo "### 📝 Changes: ℹ️ No changes to commit" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "### Update Status: ✅ Already Up to Date" >> $GITHUB_STEP_SUMMARY
            echo "Zisk ZKVM is already at the latest commit: \`$(jq -r '.zkvms.zisk.commit' config.json)\`" >> $GITHUB_STEP_SUMMARY
          fi