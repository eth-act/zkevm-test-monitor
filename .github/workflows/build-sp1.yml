name: Build SP1

on:
  workflow_call:
    inputs:
      commit_hash:
        required: true
        type: string
    outputs:
      artifact_name:
        value: ${{ jobs.build.outputs.artifact }}
      build_status:
        value: ${{ jobs.build.outputs.status }}

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      artifact: ${{ steps.artifact.outputs.name }}
      status: ${{ steps.status.outputs.value }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      - name: Build SP1 Docker Image with Caching
        run: |
          echo "Building Docker image for sp1 at commit ${{ inputs.commit_hash }}..."
          docker build \
            --build-arg REPO_URL="https://github.com/codygunton/sp1" \
            --build-arg COMMIT_HASH="${{ inputs.commit_hash }}" \
            -t "zkvm-build-sp1:${{ inputs.commit_hash }}" \
            docker/build-sp1
          
      - name: Extract SP1 Binary from Docker
        run: |
          echo "Extracting sp1 binary from Docker image..."
          mkdir -p artifacts/binaries
          docker run --rm \
            -v "$(pwd)/artifacts/binaries:/output" \
            "zkvm-build-sp1:${{ inputs.commit_hash }}"
          
      - name: Verify Binary
        run: |
          if [ ! -f "artifacts/binaries/sp1-perf-executor" ]; then
            echo "Error: Expected binary not found at artifacts/binaries/sp1-perf-executor"
            exit 1
          fi
          echo "Binary found at artifacts/binaries/sp1-perf-executor"
          ls -la artifacts/binaries/sp1-perf-executor
          # Rename for artifact upload (force overwrite)
          mv -f artifacts/binaries/sp1-perf-executor artifacts/binaries/sp1-binary
          
      - name: Upload Binary Artifact
        id: artifact
        uses: actions/upload-artifact@v4
        with:
          name: sp1-binary-${{ inputs.commit_hash }}
          path: artifacts/binaries/sp1-binary
          retention-days: 7
          
      - name: Set Status Output
        id: status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "value=success" >> $GITHUB_OUTPUT
          else
            echo "value=failure" >> $GITHUB_OUTPUT
          fi